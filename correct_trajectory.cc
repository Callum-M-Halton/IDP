switch (sensor_vals) {
    case [0,1,0]:
        //do something when var equals 1
        break;
    case [0,1,1]:
    //do something when var equals 2
        break;
    case [1,1,0]:
    //do something when var equals 2
        break;
    case [0,0,1]:
    //do something when var equals 2
        break;
    case [1,0,0]:
    //do something when var equals 2
        break;
    case [0,0,0]:
    //do something when var equals 2
        break;

  default:
    // if nothing else matches, do the default
    // default is optional
    break;
}